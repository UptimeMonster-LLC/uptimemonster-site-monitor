name: Deploy
on: [push]
jobs:
  test-build:
    name: ⚡ Test Build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
    steps:
      - name: 🔄 Checkout latest code
        uses: actions/checkout@v3

      - name: 📦 Build Dependency
        run: |
          composer install --no-dev
          composer dump-autoload -o --no-dev

      - name: 📦 Build Plugin Package
        run: |
          zip -r /tmp/${{ github.event.repository.name }}.zip .

      - name: 📦 Upload ZIP as artifact
        uses: actions/upload-artifact@v3
        with:
          name: ${{ github.event.repository.name }}
          path: zip -r /tmp/${{ github.event.repository.name }}.zip

#      - name: 📦 Post comment with ZIP download link
#        run: |
#          PR_NUMBER=$(jq --raw-output .number "${GITHUB_EVENT_PATH}")
#          ZIP_URL="https://github.com/${{ github.repository }}/suites/${{ github.run_id }}/artifacts"
#          COMMENT_BODY="Download the generated ZIP [here](${ZIP_URL})"
#
#          curl -s --request POST \
#            --url https://api.github.com/repos/${{ github.repository }}/issues/$PR_NUMBER/comments \
#            --header "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
#            --header "Content-Type: application/json" \
#            --data "{\"body\": \"$COMMENT_BODY\"}"
  deploy-dev:
    if: github.ref == 'refs/heads/develop'
    name: Deploy Development Branch
    runs-on: ubuntu-latest
    steps:
      - name: Executing Git Pull On Remote Host.
        uses: appleboy/ssh-action@master
        with:
          username: ${{ secrets.PROD_SERVER_USER }}
          host: ${{ secrets.PROD_SERVER_HOST }}
          port: ${{ secrets.PROD_SERVER_PORT }}
          key: ${{ secrets.PROD_SERVER_PRIVATE }}
          script: |
            cd ${{ secrets.STAGE_PATH }}/public/
            wp --allow-root flush-opcache flush
            cd ${{ secrets.STAGE_PATH }}/public/wp-content/plugins/uptimemonster-site-monitor/
            git status
            git checkout .
            git pull
            chown www-data:www-data -R *
            chown www-data:www-data -R .
            find . -type d -exec chmod 755 {} \;
            find . -type f -exec chmod 644 {} \;
            chown root:root .git
            chown root:root -R .git/*
            cd ${{ secrets.STAGE_PATH }}/public/
            rm -rf ${{ secrets.STAGE_PATH }}/cache/*
            wp --allow-root flush-opcache flush
            wp --allow-root rocket clean --confirm
            cd ${{ secrets.STAGE_PATH }}/public/wp-content/plugins/uptimemonster-site-monitor/
            git status
  deploy-prod:
    if: github.ref == 'refs/heads/master'
    name: Deploy Production Branch
    runs-on: ubuntu-latest
    steps:
      - name: Executing Git Pull On Remote Host.
        uses: appleboy/ssh-action@master
        with:
          username: ${{ secrets.PROD_SERVER_USER }}
          host: ${{ secrets.PROD_SERVER_HOST }}
          port: ${{ secrets.PROD_SERVER_PORT }}
          key: ${{ secrets.PROD_SERVER_PRIVATE }}
          script: |
            cd ${{ secrets.PROD_PATH }}/public/
            wp --allow-root flush-opcache flush
            cd ${{ secrets.PROD_PATH }}/public/wp-content/plugins/uptimemonster-site-monitor/
            git status
            git checkout .
            git pull
            chown www-data:www-data -R *
            chown www-data:www-data -R .
            find . -type d -exec chmod 755 {} \;
            find . -type f -exec chmod 644 {} \;
            chown root:root .git
            chown root:root -R .git/*
            cd ${{ secrets.PROD_PATH }}/public/
            rm -rf ${{ secrets.PROD_PATH }}/cache/*
            wp --allow-root flush-opcache flush
            wp --allow-root rocket clean --confirm
            cd ${{ secrets.PROD_PATH }}/public/wp-content/plugins/uptimemonster-site-monitor/
            git status
